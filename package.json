{
  "name": "github-readme-cards",
  "version": "2.0.0",
  "description": "Gera cards SVG (linguagens + stats com rank) para README do GitHub — 100% no navegador, sem servidor",
  "type": "module",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "bench": "node bench/render-bench.js"
  },
  "engines": {
    "node": ">=18"
  }
}
